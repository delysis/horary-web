{
  "name": "horary-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Horary astrology chart calculator with native local AI interpretation",
  "scripts": {
    "predev": "npm run build:ai-core:web",
    "dev": "vite",
    "lint": "eslint . && node scripts/lint.mjs",
    "prebuild": "npm run build:ai-core:web",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "pretest": "npm run build:ai-core:node && npm run build:ai-core:web",
    "test": "vitest run && node --test \"src/**/*.test.js\"",
    "pretest:react": "npm run build:ai-core:web",
    "test:react": "vitest run",
    "pretest:node": "npm run build:ai-core:node",
    "test:node": "node --test \"src/**/*.test.js\"",
    "pretest:coverage": "npm run build:ai-core:node",
    "test:coverage": "node --test --experimental-test-coverage \"src/**/*.test.js\"",
    "check": "npm run lint && npm test && npm run build",
    "build:ai-core": "node scripts/build-ai-core.mjs all",
    "build:ai-core:web": "node scripts/build-ai-core.mjs web",
    "build:ai-core:node": "node scripts/build-ai-core.mjs node",
    "check:ai-core": "cargo fmt --check --manifest-path crates/horary-ai-core/Cargo.toml && cargo test --manifest-path crates/horary-ai-core/Cargo.toml && cargo clippy --manifest-path crates/horary-ai-core/Cargo.toml -- -D warnings",
    "check:tauri": "npm run check:ai-core && cargo fmt --check --manifest-path src-tauri/Cargo.toml && cargo test --manifest-path src-tauri/Cargo.toml && cargo clippy --manifest-path src-tauri/Cargo.toml -- -D warnings",
    "check:all": "npm run check && npm run test:coverage && npm audit && npm run check:tauri",
    "check:release-assets": "node scripts/check-release-assets.mjs",
    "check:native-llama": "node scripts/check-native-llama.mjs",
    "check:horary-readings": "node scripts/check-horary-readings.mjs",
    "tauri": "tauri",
    "tauri:build:release": "tauri build --config src-tauri/tauri.release.conf.json"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4",
    "@types/node": "^24.10.1",
    "@types/react": "^19.2.7",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^5.1.1",
    "@vitest/coverage-v8": "^4.1.11",
    "eslint": "^9.39.1",
    "eslint-plugin-react-hooks": "^7.0.1",
    "eslint-plugin-react-refresh": "^0.4.24",
    "globals": "^16.5.0",
    "jsdom": "^29.1.1",
    "typescript": "~5.9.3",
    "typescript-eslint": "^8.48.0",
    "tz-lookup": "^6.1.25",
    "vite": "^7.3.1",
    "vitest": "^4.1.11"
  },
  "dependencies": {
    "@astrodraw/astrochart": "^3.0.2",
    "@litert-lm/core": "0.14.0",
    "@tauri-apps/api": "^2.11.1",
    "@tauri-apps/plugin-dialog": "^2.7.3",
    "circular-natal-horoscope-js": "^1.1.0",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  }
}
